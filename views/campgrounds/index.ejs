<% include ../partials/header %>


<header class="jumbotron jumboCamp shadow">
    <div class="container">
        <h1 class="jumboText">YelpCamp Campgrounds</h1>
        <p>We Currently Have <%= campgrounds.length %> Campgrounds Listed</p>
        <p>
            <a class="btn btn-primary btn-lg" href="campgrounds/new">Add New Campground</a>
        </p>
    </div>
</header>

<div class="row text-center equal-height" style="display:flex; flex-wrap:wrap">
    <% campgrounds.forEach(function(campground){ %>
        <div class="col-md-3 col-sm-6">
            <div class="thumbnail shadow">
              <img src="<%= campground.image %>"></img> 
              <div class="caption">
                  <h4><%= campground.name %></h4>
              </div>
              <p>
                  <a class="btn btn-primary" href="/campgrounds/<%= campground._id %>">More Info</a>
              </p>
            </div>
        </div>
    <% }); %>
 </div>


<% include ../partials/footer %>
